<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:attachments, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:attachments, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:attachment) %></h2>
    <div class="inner">
      <% form_for :attachment, url(:attachments, :create), multipart: true, :class => :form do |f| %>
        <%= partial "attachments/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
