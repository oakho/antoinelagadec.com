<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:projects, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:projects, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:project) %></h2>
    <div class="inner">
      <% form_for :project, url(:projects, :create), multipart: true, class: :form do |f| %>
        <%= partial "projects/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
